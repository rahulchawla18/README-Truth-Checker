## What this PR fixes

This PR was generated by the [readme-truth-checker](https://github.com/rahul-chawla/readme-truth-checker) Claude Code plugin after detecting that the steps in `README.md` no longer match the project's actual configuration.

A fresh `python:{PYTHON_VERSION}-slim-bookworm` container was launched, every shell command in the README was executed in order, and the failures below were diagnosed against the real codebase.

## Drift detected

{DRIFT_SUMMARY}

## Evidence

{EVIDENCE_BLOCK}

## Verification

After applying these changes, the README was re-run end-to-end in a fresh container. All previously-failing steps now pass:

```
{VERIFICATION_RESULTS}
```

## What this PR does and doesn't touch

- ✅ Only `README.md` is modified.
- ❌ No code, config, or dependencies were changed.
- ⚠️ Steps containing placeholders like `<your-token>` were skipped during verification and flagged for human review.

## How to override

If any of these suggestions are wrong (e.g., the diagnosed cause is plausible but not the real reason), just close this PR. The plugin won't re-open it automatically.

---

🤖 Generated by [readme-truth-checker](https://github.com/rahul-chawla/readme-truth-checker).
